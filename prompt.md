TO refactor the code:


Refactor the code #file-name  and make the code modular, by properly structuring and making it component based by breaking the code into components and the result code should be properly modularized. I need production grade code proeprly strucutred and modularized and refactored. First plan, structure the new changes and than build. Make the reusbale components if required. I need modulared and properly refactored code.



Code review the entire work, make sure its scalable, correct and covers edge cases
separate nested components so our components or our page.tsx files are not huge. If we have components inside of other components, we should organise them accordingly. Also, check for repeated code blocks and deduplicate anything like that. And also check for anywhere where we have any types or unknown casting because we should never do that.